# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty aside from a standard `.gitattributes` file
(text auto-detection with LF normalization). There is no source code, package
manifest, README, test suite, or build tooling yet, so there are no commands
to document and no architecture to describe.

The repository name (`finnhub-connector`) suggests it is intended to become a
connector for the [Finnhub](https://finnhub.io) financial data API, but no
implementation has been started.

## Working here

- Do not assume a language, framework, or project layout — none has been
  chosen yet. Confirm with the user before scaffolding a project structure.
- Once real code, dependencies, and tooling are added, replace this file with
  actual build/lint/test commands and a description of the architecture.
